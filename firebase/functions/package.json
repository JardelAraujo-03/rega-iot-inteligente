{
  "name": "rega-iot-api",
  "version": "1.0.0",
  "description": "API REST para Rega IoT (Firestore)",
  "main": "lib/index.js",
  "scripts": {
    "serve": "npm run build && firebase emulators:start --only functions",
    "build": "tsc",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase": "^12.5.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "typescript": "^5.3.3"
  }
}

